# macOS
.DS_Store
*.swp

# Swift Package Manager build artifacts
.build/
.swiftpm/
Packages/
*.xcodeproj
*.xcworkspace
.netrc
Package.resolved

# Built .app bundle (generated by scripts/build-app.sh / GitHub Actions)
dist/

# SPM stray artifacts (should never land in repo root, but just in case)
*.d
*.o
*.swiftdeps
*.swiftdeps~

# VS Code / IDE
.vscode/
.idea/
*.iml

# Local-only config overrides (keep config.default.json under Resources/ committed)
# Users can drop a ~/.focuspal/config.json for personal overrides.
config.local.json
